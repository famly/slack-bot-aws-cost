{
  "name": "aws-billing-to-slack",
  "description": "",
  "version": "0.1.0",
  "scripts": {
    "deploy": "serverless deploy --stage=\"prod\" --param=\"slack_url=$SLACK_WEBHOOK\" --param=\"daily_budget=1600\"",
    "invoke": "serverless invoke --function reportCostToSlack --stage=\"prod\"",
    "sls": "serverless"
  },
  "devDependencies": {
    "serverless": "3.18.2",
    "serverless-python-requirements": "6.0.0"
  }
}
